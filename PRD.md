# Cyber Bible — 提示词存储管理应用 PRD

## 1. 产品目标和使用场景

### 1.1 产品目标
Cyber Bible 是一款完全在浏览器本地运行的提示词存储管理工具，帮助用户高效地组织、检索和管理 AI 提示词，所有数据保存在本地，无需联网。

### 1.2 使用场景
- **个人知识库**：将日常积累的 AI 提示词按主题分组存储，方便复用
- **团队协作前的本地草稿**：先在本地整理好提示词，确认后再共享
- **提示词实验记录**：对每条提示词添加批注，记录使用效果和改进思路
- **快速检索**：通过关键词搜索或关系图谱快速定位所需提示词

---

## 2. 整体页面框架和导航结构

### 2.1 布局结构
采用「左侧导航 + 顶部工具栏 + 主内容区」的经典管理后台布局：

```
┌────────────────────────────────────────────────────────┐
│  [Logo] Cyber Bible          [🔍 全局搜索]  [+ 新建] │  ← 顶部工具栏
├──────────┬─────────────────────────────────────────────┤
│          │                                             │
│  首页    │           主内容区                           │
│  分组    │        （根据导航切换）                       │
│  图谱    │                                             │
│  数据库  │                                             │
│          │                                             │
└──────────┴─────────────────────────────────────────────┘
```

### 2.2 导航结构
| 导航项 | 说明 |
|--------|------|
| 首页 | 总览统计、最近添加、热门标签 |
| 分组 | 分组管理 + 提示词列表 |
| 图谱 | 关系图谱可视化 |
| 数据库 | 表格视图查看所有数据 |

---

## 3. 首页内容结构

### 3.1 统计卡片区
| 卡片 | 数据源 | 说明 |
|------|--------|------|
| 提示词总数 | prompts 表 count | 显示所有提示词数量 |
| 分组数 | groups 表 count | 显示所有分组数量 |
| 标签数 | 去重后的标签总数 | 显示所有不重复标签数量 |
| 批注数 | annotations 表 count | 显示所有批注总数 |

### 3.2 最近添加
- 按 updated 字段倒序取 5 条提示词
- 显示：标题、所属分组、更新时间
- 点击卡片跳转至提示词详情

### 3.3 热门标签
- 按使用频次降序展示标签云
- 字体大小根据使用频次动态调整
- 点击标签跳转至搜索结果（筛选该标签）

### 3.4 快捷操作
- 「新建提示词」按钮：弹出编辑面板
- 「快速搜索」输入框：聚焦到全局搜索

---

## 4. 各模块详细内容

### 4.1 分组管理模块

**布局**：左侧分组列表 + 右侧提示词卡片列表

**分组列表**：
- 显示所有分组（树形结构，支持一级嵌套）
- 每个分组显示名称和提示词数量徽章
- 操作：新建分组、重命名、删除（删除时提示是否转移提示词到其他分组）

**提示词卡片列表**：
- 显示当前分组下的所有提示词
- 每张卡片显示：标题、内容预览（前100字）、标签列表、批注数量
- 操作：点击卡片查看详情、拖拽卡片到其他分组

### 4.2 快速查找模块

**触发方式**：
- 顶部全局搜索框（所有页面可见，实时搜索）
- 导航栏搜索结果页

**搜索逻辑**：
- 匹配范围：标题、内容、标签、批注内容
- 匹配方式：包含匹配（不区分大小写）
- 结果排序：按相关度（匹配字段数量）> 更新时间倒序

**搜索结果页**：
- 高亮显示匹配的关键词
- 左侧筛选面板：按分组、标签、时间范围筛选
- 结果列表：显示匹配的提示词卡片

### 4.3 关系图谱模块

**图谱类型**：力导向图（D3.js force simulation）

**节点类型**：
| 节点类型 | 形状 | 颜色 | 说明 |
|----------|------|------|------|
| 提示词 | 圆形 | 蓝色 | 每个提示词一个节点 |
| 分组 | 方形 | 绿色 | 每个分组一个节点 |
| 标签 | 菱形 | 橙色 | 每个标签一个节点 |

**边关系**：
- 提示词 → 分组：属于关系
- 提示词 → 标签：标记关系

**交互操作**：
- 拖拽节点调整布局
- 滚轮缩放 / 拖拽平移画布
- 点击节点：高亮其所有关联节点，右侧面板显示详情
- 双击节点：跳转到提示词详情页
- 图例说明：显示各节点类型对应的形状和颜色

### 4.4 提示词详情/编辑模块

**详情模式**：
- 标题（大字号）
- 完整内容（代码块样式，支持一键复制）
- 所属分组（可点击切换）
- 标签列表（可点击移除）
- 批注列表（按时间倒序，每条显示时间和内容）

**编辑模式**：
- 标题输入框
- 内容文本域（支持多行）
- 分组选择器（下拉选择）
- 标签输入框（支持自动补全已有标签，回车添加）

**批注区**：
- 底部独立区域
- 添加批注：输入框 + 添加按钮
- 每条批注显示时间和内容
- 操作：编辑批注、删除批注

### 4.5 可视化数据库模块

**表格视图**：
- 列：标题、分组、标签、创建时间、修改时间、批注数
- 支持按任意列排序（点击表头）
- 支持按分组/标签筛选
- 操作列：查看、编辑、删除

**数据操作**：
- 导出为 JSON：将所有数据导出为 JSON 文件（备份用途）
- 导入 JSON：从 JSON 文件恢复数据（合并或覆盖）

---

## 5. 第一版必要的数据和操作

### 5.1 数据表结构（IndexedDB）

| 表名 | 字段 | 说明 |
|------|------|------|
| prompts | id, title, content, groupId, tags[], created, updated | 提示词主表 |
| groups | id, name, color, order | 分组表 |
| annotations | id, promptId, content, created | 批注表 |

### 5.2 必要操作

| 模块 | 操作 |
|------|------|
| 提示词 | 新建、查看、编辑、删除、复制内容 |
| 分组 | 新建、重命名、删除 |
| 批注 | 添加、编辑、删除 |
| 标签 | 添加、移除（通过提示词编辑） |
| 搜索 | 全局搜索、筛选 |
| 数据 | 导出 JSON、导入 JSON |

---

## 6. 各模块之间的关系

```
首页 ──→ 统计信息 ←── prompts / groups / annotations 表
  │
  └─→ 最近添加 ─────→ 提示词详情 ←── 分组管理模块
                          ↑
分组管理 ──→ 提示词列表 ──┘
  │
  └─→ 分组操作（CRUD）

快速查找 ──→ 搜索结果 ──→ 提示词详情

关系图谱 ──→ 点击节点 ──→ 提示词详情

可视化数据库 ──→ 表格行操作 ──→ 提示词详情
```

所有模块最终都汇聚到**提示词详情/编辑模块**，它是核心交互节点。

---

## 7. 本地数据文件、备份和恢复需求

### 7.1 数据存储
- 使用浏览器原生 IndexedDB，数据库名为 CyberBibleDB
- 数据完全保存在用户浏览器本地，不上传任何服务器
- 刷新页面、关闭后重新打开，数据不丢失

### 7.2 备份（导出）
- 在「可视化数据库」模块提供「导出 JSON」按钮
- 导出文件包含所有表数据（prompts、groups、annotations）
- 文件名格式：cyber_bible_backup_YYYYMMDD.json

### 7.3 恢复（导入）
- 在「可视化数据库」模块提供「导入 JSON」按钮
- 支持两种模式：
  - **合并**：保留现有数据，仅添加新数据（按 id 去重）
  - **覆盖**：清空现有数据后导入
- 导入前进行 JSON 格式校验

### 7.4 数据安全提醒
- 清除浏览器缓存/数据会导致数据丢失，需提示用户使用备份功能
- 首次使用时显示简短的数据安全提示

---

## 8. 第一版必须完成的功能

- [ ] 页面框架：左侧导航 + 顶部工具栏 + 主内容区
- [ ] 首页总览：统计卡片、最近添加、热门标签
- [ ] 分组管理：分组 CRUD、分组下的提示词列表
- [ ] 提示词 CRUD：新建、查看、编辑、删除提示词
- [ ] 标签功能：添加/移除标签、标签自动补全
- [ ] 批注功能：添加/编辑/删除批注
- [ ] 全局搜索：实时搜索 + 结果高亮 + 筛选
- [ ] 关系图谱：力导向图展示、节点交互、详情面板
- [ ] 可视化数据库：表格视图、排序、筛选
- [ ] 数据导出/导入：JSON 格式的备份与恢复
- [ ] IndexedDB 数据持久化：刷新和重启后数据保留

---

## 9. 第一版暂时不做的功能

- 云同步 / 账号系统
- 多设备数据同步
- 提示词版本历史（每次编辑记录快照）
- 分享功能（生成分享链接）
- 提示词模板变量（如 {{name}} 占位符替换）
- 多语言国际化
- 深色/浅色主题切换
- 拖拽排序（分组和提示词的顺序调整）
- 提示词收藏/置顶
- 批量操作（批量删除、批量移动分组）
- 快捷键支持
- PWA 离线安装

---

## 10. 验收标准

### 10.1 功能验收
| # | 验收项 | 验收标准 |
|---|--------|----------|
| 1 | 数据持久化 | 添加提示词后刷新页面，数据仍在 |
| 2 | 数据持久化 | 关闭浏览器后重新打开应用，数据仍在 |
| 3 | 分组 CRUD | 创建分组 A，重命名为 B，删除 B，提示词随之处理 |
| 4 | 提示词 CRUD | 新建提示词 → 编辑内容 → 保存 → 刷新后确认保存成功 |
| 5 | 标签功能 | 为提示词添加标签「测试」，在搜索框输入「测试」能找到该提示词 |
| 6 | 批注功能 | 为提示词添加批注 → 编辑批注 → 删除批注，操作后数据正确 |
| 7 | 全局搜索 | 搜索提示词标题中的关键词，结果正确高亮 |
| 8 | 关系图谱 | 页面加载后显示力导向图，节点可拖拽，点击显示详情 |
| 9 | 数据库视图 | 表格显示所有提示词，可按列排序 |
| 10 | 数据导出 | 点击导出，下载 JSON 文件，文件内容包含所有数据 |
| 11 | 数据导入 | 导出的 JSON 文件能成功导入，数据一致 |
| 12 | 导航切换 | 点击左侧各导航项，主内容区正确切换 |

### 10.2 性能验收
| # | 验收项 | 验收标准 |
|---|--------|----------|
| 1 | 页面加载 | 首页在 2 秒内完成渲染（100条提示词以内） |
| 2 | 搜索响应 | 搜索框输入后 300ms 内显示结果 |
| 3 | 图谱渲染 | 100 个节点以内图谱流畅拖拽（无卡顿） |

### 10.3 兼容性验收
| # | 验收项 | 验收标准 |
|---|--------|----------|
| 1 | 浏览器 | 最新版 Chrome / Edge / Firefox 正常运行 |
| 2 | 分辨率 | 1366×768 及以上分辨率布局正常 |
